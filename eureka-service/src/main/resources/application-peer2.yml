server:
  port: 1112

spring:
  application:
    name: eureka-service

eureka:
  instance:
    hostname: peer2
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://peer1:1111/eureka/



